#include "Fixed.hpp"

int main()
{
	// Fixed a (2.43f);
	// Fixed b (12.43f);

	// Fixed c = a + b;
	// std::cout << "+: " << c.toFloat() << '\n';

	// c = a - b;
	// std::cout << "-: " << c.toFloat()  << '\n';

	// c = a * b;
	// std::cout << "*: " << c.toFloat()  << '\n';

	// c = a / b;
	// std::cout << "/: " << c.toFloat()  << '\n';

	// std::cout << "before ++: " << a.toFloat() << '\n';
	// a++;
	// std::cout << "++: " << a.toFloat() << '\n';
	// a--;
	// std::cout << "--: " << a.toFloat() << '\n';
	// ++a;
	// std::cout << "pre ++: " << a.toFloat() << '\n';

	// std::cout << "a: " << a.toFloat() << '\n';
	// c = a--;
	// std::cout << "c: " << c.toFloat() << '\n';
	// std::cout << "new a: " << a.toFloat() << '\n';

	// --a;
	// std::cout << "pre --: " << a.toFloat() << '\n';
	// Fixed a;
	// Fixed const b(Fixed(5.05f) * Fixed(2));
	// std::cout << a << std::endl;
	// std::cout << ++a << std::endl;
	// std::cout << a << std::endl;
	// std::cout << a++ << std::endl;
	// std::cout << a << std::endl;
	// std::cout << b << std::endl;
	// std::cout << Fixed::max(a, b) << std::endl;

}